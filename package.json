{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node ./enforce-pnpm.cjs",
    "dev": "node ./dev-all.mjs",
    "dev:parallel": "node ./dev-all.mjs",
    "build": "pnpm run typecheck && pnpm --filter @workspace/api-server --filter @workspace/roi-platform run build",
    "build:all": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
